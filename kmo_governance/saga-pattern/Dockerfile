# Auto-generated by Architekt 2026-04-30 fuer Phase-5 DEV-Stage
FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends gcc && rm -rf /var/lib/apt/lists/*
COPY requirements.txt* ./
RUN pip install --no-cache-dir pyyaml pytest 2>/dev/null || true
COPY . /app
ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app
# Health: simple python import check
HEALTHCHECK --interval=30s --timeout=5s CMD python -c "import sys; sys.exit(0)"
# Default CMD: pytest run (Service-Stub, replace per build-spezifisch)
CMD ["python", "-c", "import time; print(\"KMO sibling stub running\"); time.sleep(86400)"]

