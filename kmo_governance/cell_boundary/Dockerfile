# Auto-generated by Architekt 2026-05-01 fuer Welle-9-alpha Phase-1.2.1 Cell-Boundary
FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends gcc && rm -rf /var/lib/apt/lists/*
COPY requirements.txt* ./
RUN pip install --no-cache-dir pytest 2>/dev/null || true
COPY . /app
ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app
HEALTHCHECK --interval=30s --timeout=5s CMD python -c "import sys; sys.exit(0)"
CMD ["python", "-c", "import time; print('KMO cell-boundary stub running'); time.sleep(86400)"]
